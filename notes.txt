pseudo class :
hover, active

css properties :
opacity, box-shadow, transition(value is a css prop.)

Box-model:
padding(inside the element)
margin (outside the element)

<p> by default, comes with margin at top and bottom

types of elements:
block elements
in-line block elements
in-line elements


grid
grid-templatte-coulumns
fr(free space)

Flexbox
flex-direction
flex : 1 (eq. to 1fr)

horizontal alignment : justify-content : center;
vertical alignment : align-items : center;



Grid is rigid. Flexbox is flexible.

flex-shrink : 0; don't shrink
width : 0; shrink


position
fixed : placed in the browser window
absolute : placed on the page
when position absoloute is placed inside 
fixed element it is set relative to that.
